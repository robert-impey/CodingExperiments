/**
 * A little program that takes a directory name as an argument
 * and then searches the directory for specially formatted text files.
 *
 * The first line of each text file is a command that can be given
 * to the shell.
 *
 * Then there is a blank line.
 *
 * Then there is the expected output.
 *
 * Foreach text file, the file name and command are printed, along with
 * 'OK' for success and 'FAIL' otherwise.
 *
 * @copyright 2010-03-20, Robert Impey
 */

#include <stdio.h>

int main()
{
	return 0;
}
